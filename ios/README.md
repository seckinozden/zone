# Zone iOS

SwiftUI app for Zone. Deferred — planned after backend + web are stable.

## Planned

- SwiftUI app targeting iOS 17+
- Reads from the same `/api` the web app uses
- Same auth as web (decision still pending)
- Day-focused view: wake up, see today's schedule at a glance

## Status

Not started. Folder reserved so the monorepo structure (`backend/`, `web/`, `ios/`, `design/`) is complete from day one.
