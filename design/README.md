# Design

Drop Google Stitch exports (screens, components, assets) into this folder. The web UI will be built to match these.

Suggested layout:
- `screens/` — per-screen exports (PNG/PDF/HTML)
- `components/` — component-level references
- `tokens/` — colors, spacing, type if Stitch exports them
- `assets/` — icons, images referenced by screens
